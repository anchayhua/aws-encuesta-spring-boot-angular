===== CREAMOS NUESTRO REPOSITORIO EN GITHUB =======
1. Clonar con los procedimientos normales con git bash
	- git clone
	- git add
	- git commit
	- git push origin master
2. Generar SSH para autenticarnos en nuestro local
	- ssh-keygen -t rsa -b 4096 -C "ncnr_quique@hotmail.com"
	- cat ~/.ssh/id_rsa.pub


======================= ANGULAR ====================
1. Actualizamos el npm 
	- npm install -g npm


Para iniciar mysql

Start MySQL
sudo /usr/local/mysql/support-files/mysql.server start

Stop MySQL
sudo /usr/local/mysql/support-files/mysql.server stop

Restart MySQL
sudo /usr/local/mysql/support-files/mysql.server restart